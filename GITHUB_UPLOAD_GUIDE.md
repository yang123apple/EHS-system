# GitHub 上传指南

## 项目已准备就绪 ✅

你的 EHS 工作许可管理系统已经成功初始化 Git 仓库并创建了初始提交。

## 上传到 GitHub 的步骤

### 1. 在 GitHub 上创建新仓库

1. 访问 https://github.com/new
2. 填写仓库信息：
   - **仓库名称**: `ehs-work-permit-system` （或你喜欢的名称）
   - **描述**: `EHS工作许可管理系统 - 支持动态工作流、多步审批、Excel样式表单渲染`
   - **可见性**: 
     - Public（公开） - 任何人都可以看到
     - Private（私有） - 只有你和授权的人可以访问
   - **不要勾选** "Initialize this repository with a README"（因为我们已经有了）
3. 点击 "Create repository"

### 2. 连接到远程仓库

在 PowerShell 中执行以下命令（将 `YOUR_USERNAME` 替换为你的 GitHub 用户名）：

```powershell
# 添加远程仓库
git remote add origin https://github.com/YOUR_USERNAME/ehs-work-permit-system.git

# 查看远程仓库配置
git remote -v
```

### 3. 推送代码到 GitHub

```powershell
# 推送到 main 分支（GitHub 新仓库默认是 main，但我们本地是 master）
git branch -M main
git push -u origin main
```

**首次推送时**，系统会要求你登录 GitHub：
- 输入你的 GitHub 用户名
- 输入你的个人访问令牌（Personal Access Token）

> 💡 **注意**: 自 2021 年起，GitHub 不再接受密码认证，需要使用个人访问令牌。

### 4. 创建个人访问令牌（如果还没有）

1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token" → "Generate new token (classic)"
3. 设置令牌：
   - **Note**: `EHS System Upload`
   - **Expiration**: 选择过期时间（建议 90 days）
   - **Select scopes**: 勾选 `repo` （完整的仓库访问权限）
4. 点击 "Generate token"
5. **复制生成的令牌**（只会显示一次！）
6. 在推送时，使用令牌作为密码

### 5. 验证上传成功

推送成功后，访问你的 GitHub 仓库页面：
```
https://github.com/YOUR_USERNAME/ehs-work-permit-system
```

你应该能看到所有的代码文件！

## 后续更新代码

当你修改代码后，使用以下命令更新到 GitHub：

```powershell
# 查看修改的文件
git status

# 添加所有修改
git add .

# 创建提交
git commit -m "描述你的修改内容"

# 推送到 GitHub
git push
```

## 项目信息

- **本地仓库**: `C:\Users\Guangyang\Desktop\ESH_system\ehs-system1.0`
- **分支**: main
- **已提交文件**: 121 个文件，26196 行代码
- **初始提交**: `Initial commit: EHS Work Permit Management System V3.2`

## 快速命令参考

```powershell
# 查看仓库状态
git status

# 查看提交历史
git log --oneline

# 查看远程仓库
git remote -v

# 拉取远程更新
git pull

# 推送到远程
git push
```

## 需要帮助？

如果遇到问题，可以参考：
- GitHub 官方文档: https://docs.github.com/
- Git 基础教程: https://git-scm.com/book/zh/v2

---

✨ **准备好了！现在去 GitHub 创建仓库并执行第 2-3 步的命令即可！**
