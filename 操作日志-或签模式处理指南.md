# 操作日志系统 - 或签模式处理指南

## 📋 什么是或签模式

**或签**（OR签署）是一种工作流审批模式，在某个步骤中：
- 设置多个候选处理人（如：张三、李四、王五）
- 只要**任意一人**完成操作，该步骤即完成
- 其他候选人的操作权限自动失效
- 记录实际操作人，并推进到下一步骤

### 典型场景

1. **隐患验收**：安全员A、安全员B、安全员C都可以验收，任意一人验收即可
2. **许可证审批**：部门经理A、部门经理B都可以审批，谁先审批谁完成
3. **培训审核**：培训专员A、培训专员B都可以审核

## 🎯 或签模式的日志记录要点

### 1. 记录候选处理人列表

在派发或进入或签步骤时，需要记录所有候选处理人：

```typescript
await ActivityLogger.logAction({
  userId: dispatcher.id,
  action: 'DISPATCH',
  actionLabel: '派发（或签）',
  module: Modules.HAZARD,
  targetType: 'hazard',
  targetId: hazard.code,
  targetLabel: hazard.desc?.substring(0, 50),
  details: `派发到验收步骤（或签模式），候选处理人：张三、李四、王五`,
  afterData: {
    step: '验收',
    mode: 'OR_SIGN',
    candidates: [
      { id: 'user1', name: '张三' },
      { id: 'user2', name: '李四' },
      { id: 'user3', name: '王五' }
    ],
    assignedAt: new Date().toISOString()
  },
  request,
});
```

### 2. 记录实际操作人

当某个候选人完成操作时，记录实际操作人：

```typescript
// 张三完成了验收操作
await ActivityLogger.logAction({
  userId: 'user1',  // 张三的ID
  action: 'VERIFY',
  actionLabel: '验收通过（或签）',
  module: Modules.HAZARD,
  targetType: 'hazard',
  targetId: hazard.code,
  targetLabel: hazard.desc?.substring(0, 50),
  details: `张三完成验收（或签模式，候选人：张三、李四、王五）`,
  beforeData: {
    status: '待验收',
    candidates: ['张三', '李四', '王五']
  },
  afterData: {
    status: '已验收',
    actualOperator: '张三',
    operatorId: 'user1',
    completedAt: new Date().toISOString()
  },
  snapshot: {
    mode: 'OR_SIGN',
    allCandidates: [
      { id: 'user1', name: '张三', operated: true },
      { id: 'user2', name: '李四', operated: false },
      { id: 'user3', name: '王五', operated: false }
    ],
    actualOperator: { id: 'user1', name: '张三' }
  },
  request,
});
```

### 3. 记录其他候选人权限失效（可选）

可以记录其他候选人的权限被自动撤销：

```typescript
// 可选：记录权限变更
await ActivityLogger.logAction({
  userId: 'system',
  action: 'REVOKE_PERMISSION',
  actionLabel: '撤销操作权限',
  module: Modules.HAZARD,
  targetType: 'hazard',
  targetId: hazard.code,
  targetLabel: hazard.desc?.substring(0, 50),
  details: `或签步骤已完成（张三），自动撤销其他候选人（李四、王五）的操作权限`,
  snapshot: {
    completedBy: '张三',
    revokedUsers: ['李四', '王五']
  },
  request,
});
```

## 💡 完整示例 - 隐患验收（或签）

### 步骤1：派发到验收步骤（或签模式）

```typescript
// src/app/api/hazards/[id]/dispatch-verify/route.ts
export async function POST(
  request: NextRequest,
  { params }: { params: { id: string } }
) {
  try {
    const user = await getUser(request);
    const body = await request.json();
    const { verifierIds } = body;  // 多个验收人ID数组

    // 获取验收人信息
    const verifiers = await prisma.user.findMany({
      where: { id: { in: verifierIds } },
      select: { id: true, name: true }
    });

    // 更新隐患记录
    const hazard = await prisma.hazardRecord.update({
      where: { id: params.id },
      data: {
        status: '待验收',
        verifierIds: verifierIds,  // 存储多个候选人
        verifierMode: 'OR_SIGN',   // 标记为或签模式
      }
    });

    // 记录派发日志
    await ActivityLogger.logAction({
      userId: user.id,
      action: 'DISPATCH',
      actionLabel: '派发验收（或签）',
      module: Modules.HAZARD,
      targetType: 'hazard',
      targetId: hazard.code,
      targetLabel: hazard.desc?.substring(0, 50),
      details: `派发到验收步骤（或签模式），候选验收人：${verifiers.map(v => v.name).join('、')}`,
      afterData: {
        status: '待验收',
        step: '验收',
        mode: 'OR_SIGN',
        candidates: verifiers,
        assignedAt: new Date().toISOString(),
        assignedBy: user.name
      },
      snapshot: {
        workflowMode: 'OR_SIGN',
        candidateCount: verifiers.length,
        allCandidates: verifiers
      },
      request,
    });

    // 发送通知给所有候选人
    for (const verifier of verifiers) {
      await createNotification({
        userId: verifier.id,
        type: 'hazard_verify_pending',
        title: '隐患待验收（或签）',
        content: `隐患 ${hazard.code} 待您验收（或签模式，多人中任意一人操作即可）`,
        relatedType: 'hazard',
        relatedId: hazard.id
      });
    }

    return NextResponse.json({
      success: true,
      data: hazard
    });
  } catch (error) {
    console.error('派发验收失败:', error);
    return NextResponse.json(
      { error: '派发失败' },
      { status: 500 }
    );
  }
}
```

### 步骤2：某个验收人完成验收

```typescript
// src/app/api/hazards/[id]/verify/route.ts
export async function POST(
  request: NextRequest,
  { params }: { params: { id: string } }
) {
  try {
    const user = await getUser(request);
    const body = await request.json();
    const { approved, comment } = body;

    // 获取隐患记录
    const beforeData = await prisma.hazardRecord.findUnique({
      where: { id: params.id }
    });

    if (!beforeData) {
      return NextResponse.json({ error: '隐患不存在' }, { status: 404 });
    }

    // 检查是否为候选验收人
    if (!beforeData.verifierIds?.includes(user.id)) {
      return NextResponse.json({ error: '无权操作' }, { status: 403 });
    }

    // 获取所有候选人信息（用于日志）
    const allCandidates = await prisma.user.findMany({
      where: { id: { in: beforeData.verifierIds } },
      select: { id: true, name: true }
    });

    // 更新隐患记录
    const newStatus = approved ? '已验收' : '待整改';
    const afterData = await prisma.hazardRecord.update({
      where: { id: params.id },
      data: {
        status: newStatus,
        verifierId: user.id,        // 实际操作人
        verifierName: user.name,
        verifyComment: comment,
        verifyTime: new Date(),
        verifierMode: null,         // 清除或签模式标记
        verifierIds: null,          // 清除候选人列表
      }
    });

    // 记录验收日志（包含或签信息）
    await ActivityLogger.logApproval({
      userId: user.id,
      module: Modules.HAZARD,
      targetType: 'hazard',
      targetId: afterData.code,
      targetLabel: afterData.desc?.substring(0, 50),
      action: approved ? 'APPROVE' : 'REJECT',
      comment,
      beforeStatus: beforeData.status,
      afterStatus: newStatus,
      request,
    });

    // 额外记录或签完成信息
    await ActivityLogger.logAction({
      userId: user.id,
      action: 'OR_SIGN_COMPLETED',
      actionLabel: '或签完成',
      module: Modules.HAZARD,
      targetType: 'hazard',
      targetId: afterData.code,
      targetLabel: afterData.desc?.substring(0, 50),
      details: `${user.name}完成验收（或签模式），候选人：${allCandidates.map(c => c.name).join('、')}`,
      beforeData: {
        status: beforeData.status,
        verifierMode: 'OR_SIGN',
        candidates: allCandidates.map(c => c.name)
      },
      afterData: {
        status: newStatus,
        actualOperator: user.name,
        operatorId: user.id,
        completedAt: new Date().toISOString()
      },
      snapshot: {
        mode: 'OR_SIGN',
        allCandidates: allCandidates.map(c => ({
          ...c,
          operated: c.id === user.id
        })),
        actualOperator: { id: user.id, name: user.name },
        notOperatedUsers: allCandidates
          .filter(c => c.id !== user.id)
          .map(c => c.name)
      },
      request,
    });

    // 通知其他候选人权限已失效
    const otherCandidates = allCandidates.filter(c => c.id !== user.id);
    for (const candidate of otherCandidates) {
      await createNotification({
        userId: candidate.id,
        type: 'or_sign_completed_by_other',
        title: '或签步骤已完成',
        content: `隐患 ${afterData.code} 的验收已由${user.name}完成，您的操作权限已失效`,
        relatedType: 'hazard',
        relatedId: afterData.id
      });
    }

    return NextResponse.json({
      success: true,
      data: afterData
    });
  } catch (error) {
    console.error('验收失败:', error);
    return NextResponse.json(
      { error: '验收失败' },
      { status: 500 }
    );
  }
}
```

## 🔍 查询或签操作日志

### 查看某个隐患的或签历史

```typescript
const logs = await prisma.systemLog.findMany({
  where: {
    targetType: 'hazard',
    targetId: 'HZ-2024-001',
    OR: [
      { action: 'DISPATCH', details: { contains: '或签' } },
      { action: 'OR_SIGN_COMPLETED' }
    ]
  },
  orderBy: { createdAt: 'desc' }
});

// 分析或签操作
logs.forEach(log => {
  if (log.action === 'DISPATCH') {
    const snapshot = JSON.parse(log.snapshot);
    console.log('派发时的候选人:', snapshot.allCandidates);
  }
  if (log.action === 'OR_SIGN_COMPLETED') {
    const snapshot = JSON.parse(log.snapshot);
    console.log('实际操作人:', snapshot.actualOperator);
    console.log('未操作人员:', snapshot.notOperatedUsers);
  }
});
```

### 在UI中展示或签信息

```typescript
// src/components/HazardLogViewer.tsx
function renderOrSignLog(log: ActivityLog) {
  if (log.action === 'OR_SIGN_COMPLETED' && log.snapshot) {
    const snapshot = log.snapshot;
    
    return (
      <div className="bg-purple-50 border border-purple-200 rounded-lg p-4">
        <div className="flex items-center gap-2 mb-2">
          <Users size={16} className="text-purple-600" />
          <span className="font-medium text-purple-900">或签完成</span>
        </div>
        
        <div className="space-y-2 text-sm">
          <div>
            <span className="text-slate-600">候选人：</span>
            <span className="font-medium">
              {snapshot.allCandidates.map(c => c.name).join('、')}
            </span>
          </div>
          
          <div>
            <span className="text-slate-600">实际操作人：</span>
            <span className="font-medium text-green-600">
              {snapshot.actualOperator.name}
            </span>
          </div>
          
          {snapshot.notOperatedUsers?.length > 0 && (
            <div>
              <span className="text-slate-600">未操作人员：</span>
              <span className="text-slate-500">
                {snapshot.notOperatedUsers.join('、')}
              </span>
            </div>
          )}
        </div>
      </div>
    );
  }
  
  return null;
}
```

## 📊 数据库设计建议

### 1. 在业务表中添加或签相关字段

```prisma
model HazardRecord {
  // ... 其他字段
  
  // 或签模式支持
  verifierMode    String?    // 'OR_SIGN' | 'AND_SIGN' | null
  verifierIds     String[]   // 候选验收人ID数组
  verifierId      String?    // 实际操作人ID
  verifierName    String?    // 实际操作人姓名
  
  // 作业许可同理
  approverMode    String?
  approverIds     String[]
  approverId      String?
  approverName    String?
}
```

### 2. 日志快照字段建议结构

```typescript
interface OrSignSnapshot {
  mode: 'OR_SIGN';
  allCandidates: Array<{
    id: string;
    name: string;
    operated: boolean;  // 是否已操作
  }>;
  actualOperator?: {
    id: string;
    name: string;
  };
  notOperatedUsers?: string[];  // 未操作人员姓名列表
  candidateCount: number;
  completedAt?: string;
}
```

## 🎨 UI展示建议

### 1. 在详情页显示或签状态

```typescript
{hazard.verifierMode === 'OR_SIGN' && (
  <div className="bg-purple-50 border border-purple-200 rounded-lg p-4">
    <div className="flex items-center gap-2 mb-2">
      <Users size={20} className="text-purple-600" />
      <span className="font-medium text-purple-900">或签模式</span>
    </div>
    <p className="text-sm text-slate-600">
      候选验收人：{verifiers.map(v => v.name).join('、')}
    </p>
    <p className="text-xs text-slate-500 mt-1">
      任意一人完成验收即可推进流程
    </p>
  </div>
)}

{hazard.verifierId && hazard.verifierMode === null && (
  <div className="text-sm text-slate-600">
    <span>实际验收人：</span>
    <span className="font-medium text-green-600">{hazard.verifierName}</span>
  </div>
)}
```

### 2. 在操作日志中高亮或签操作

```typescript
function getOrSignBadge(log: ActivityLog) {
  if (log.details?.includes('或签') || log.action === 'OR_SIGN_COMPLETED') {
    return (
      <span className="inline-flex items-center gap-1 px-2 py-0.5 bg-purple-100 text-purple-700 rounded text-xs">
        <Users size={12} />
        或签
      </span>
    );
  }
  return null;
}
```

## 📝 字段标签映射

建议为或签相关字段添加中文标签：

```typescript
// src/utils/fieldLabels.ts
export const HazardFieldLabels = {
  // ... 其他字段
  verifierMode: '验收模式',
  verifierIds: '候选验收人',
  verifierId: '实际验收人',
  verifierName: '验收人姓名',
  
  approverMode: '审批模式',
  approverIds: '候选审批人',
  approverId: '实际审批人',
  approverName: '审批人姓名',
};
```

## ⚠️ 注意事项

1. **并发控制**
   - 多个候选人可能同时操作
   - 使用数据库事务和乐观锁
   - 第一个成功的操作完成该步骤

2. **权限验证**
   - 检查操作人是否在候选人列表中
   - 检查步骤是否还处于待处理状态
   - 已完成的步骤不能再次操作

3. **通知机制**
   - 派发时通知所有候选人
   - 完成时通知其他候选人权限失效
   - 清晰说明或签模式

4. **日志完整性**
   - 记录所有候选人信息
   - 记录实际操作人
   - 记录未操作人员
   - 便于后续审计和追溯

5. **UI提示**
   - 明确标识或签模式
   - 显示所有候选人
   - 高亮实际操作人
   - 提示其他人权限失效

## 🎯 总结

或签模式的日志记录关键点：

1. **派发阶段**：记录所有候选处理人
2. **完成阶段**：记录实际操作人和未操作人员
3. **快照数据**：保存完整的候选人列表和操作状态
4. **通知机制**：及时通知相关人员
5. **UI展示**：清晰展示或签状态和结果

这样可以确保或签模式下的操作历史完整、清晰、可追溯！

---
更新时间：2026-01-02
